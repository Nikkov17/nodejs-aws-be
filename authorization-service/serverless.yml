service: authorization-service

plugins:
  - serverless-dotenv-plugin

frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: eu-west-1

functions:
  basicAuthorizer:
    handler: ./basicauthorizer.basicAuthorizer
